
public class Knight extends Piece {

    public Knight (int location, String fen) {
        super(location, fen);
        
        //this.sprite = sheet.getSubimage(3 * sheetScale, Character.isUpperCase((fen.charAt(0))) ? 0 : sheetScale, sheetScale, sheetScale).getScaledInstance(sheetScale, sheetScale, BufferedImage.SCALE_SMOOTH);
    }
}